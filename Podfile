# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CallbackFunctionsTesting' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CallbackFunctionsTesting
  pod 'Alamofire'
  pod 'ObjectMapper'
  pod 'AlamofireObjectMapper'

  target 'CallbackFunctionsTestingTests' do
    inherit! :search_paths

    pod 'Quick'
    pod 'Nimble'
    # Pods for testing
  end

  target 'CallbackFunctionsTestingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
